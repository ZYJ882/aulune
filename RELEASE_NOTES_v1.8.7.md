# Aulune v1.8.7

## 关键操作流程优化

Aulune v1.8.7 优化了从内容浏览到模型配置的关键操作链路。内容库在为空时，“去首页”按钮现在会直接打开“灵感”页；对话页中的服务商状态条可直接进入模型设置，未配置时会提供清晰的下一步提示；本机画像页则会在云端 AI 尚不可用时跳转至配置页，避免发起无效更新请求。

模型工作台新增更严格的启用前置条件：必须填写 API Key 才能启用云端调用。切换服务商时，空 API Key 不会继承上一服务商的 Key。清除配置改为仅影响当前服务商，并在删除前要求二次确认；当该服务商正用于云端增强时，对应增强也会停止。

## 验证结果

本版本在本地使用 Android SDK 35 与 Java 17 构建。`app-debug.apk` 已通过 APK Signature Scheme v2 签名校验；Android 单元测试全部通过，共 47 项。

## 安装说明

> **重要：本次 v1.8.7 虽作为正式 Release 发布，但 APK 使用临时 Android Debug 证书签名。它不能覆盖安装既有使用固定发布证书签名的版本，包括 v1.8.6。**
>
> 如设备已安装旧正式版，请先在系统设置中卸载旧版，再安装 `Aulune-v1.8.7.apk`。卸载应用会清除其本机内容库、对话、画像和已保存的云端配置；如需保留数据，请先自行备份。

## 发布资产

| 资产 | 说明 |
|---|---|
| `Aulune-v1.8.7.apk` | 本地构建的临时 Debug 签名 Android 安装包。 |
| `Aulune-v1.8.7-SHA256SUMS.txt` | APK 与源码归档的 SHA-256 校验清单。 |
| `Aulune-v1.8.7-source.zip` | 与本 Release 对应的源码归档。 |
